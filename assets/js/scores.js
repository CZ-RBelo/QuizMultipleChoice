


/*
numCorrects


// Clear the old questions
questionChoices.innerHTML = "";


// Set the question title
questionTitle.textContent = quizQuestions[currentQuestionIndex].questTitle;

*/


var userInitials = "";
var userResult = 0; 

function displayHS(userInitials, userResult)
{

    var x = document.getElementById("myText").value;


    if (initialsInp.value.length > 0) {

        console.log(initialsInp.value);
        console.log(numCorrects);

        //window.location.href = "highscores.html";
    };
    */


console.log(userInitials);
console.log(userResult);

};

/*
let storeInitials = (...input) => {
    let data = JSON.stringify({ "name":input[0], "score":input[1]})
    localStorage.setItem("object", data)
}
storeInitials(initialBox.value, userScore);

var playAgain = document.createElement("button");
playAgain.textContent= "Play Again!";
blank.appendChild(playAgain);

playAgain.addEventListener("click", () => {
    location.reload();
})
*/